"use client";

import type { Stock } from "@/lib/types";

export function NotificationCenter({ stocks }: { stocks: Stock[] }) {
  // Los toast flotantes fueron eliminados para no bloquear la experiencia.
  // Este componente queda como placeholder para futuras notificaciones no intrusivas.
  return null;
}
